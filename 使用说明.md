# Zw Voice Factory 使用说明

## 1. 软件定位

Zw Voice Factory 是一个面向长篇小说的 Windows 本地多角色配音工作台。它把小说导入、章节切片、角色识别、角色审核、参考音频、导演脚本、语音生成、RVC 音色稳定、流式试听和音量统一放进同一个项目工作区。

它解决的不是“把一段文字送进 TTS”这一单点问题，而是长篇配音中的几个连续问题：

- 同一个角色跨章节保持稳定的声线和身份。
- 把旁白、角色对白和路人对白分开处理，降低说话人误判。
- 长文本按批次处理，第一批可以先试听，后续批次在后台继续准备。
- 参考音频、导演文件、生成音频和 RVC 资产可以复用、审核、删除或从失败处继续。
- 多个本地模型由统一启动器管理，避免服务孤立启动、进度不可见和关闭后残留进程。

项目定位是 Local-first 的单机生产工具，不是在线 SaaS，也不包含 GPT-SoVITS、RVC 或其他第三方模型本身。

## 2. 运行条件

### 基础环境

- Windows 10/11 64 位。
- Python 3.11 或更高版本。
- Node.js 20 或更高版本，包含 `npm`。
- .NET 8 SDK（只有重新编译图形启动器时需要；仓库已提供 `ZwVoiceFactoryLauncher.exe`）。
- PowerShell 5.1 或 PowerShell 7。
- 足够的项目盘空间。模型和运行环境必须放在项目目录所在磁盘，不会自动搬到 C 盘。

### 硬件建议

- 文本分析可以使用本地 Ollama；显存不足时速度会下降，但不影响项目文件管理。
- GPT-SoVITS、VoxCPM2 和 RVC 训练更适合 NVIDIA GPU。不同版本的模型显存要求不同，应以对应模型的官方说明为准。
- IndexTTS2 在本项目中作为按需服务，运行速度和可用性取决于本机 CPU/GPU 与安装环境。
- 长篇分析、云端 API 和音频生成需要较大的磁盘空间。建议把仓库、模型、缓存和输出放在空间充足的非系统盘。

## 3. 首次部署

以下命令在仓库根目录执行。若仓库位于 `G:\Desktop\Zw_factory\Zw_Voice_factory`，请先进入该目录：

```powershell
Set-Location G:\Desktop\Zw_factory\Zw_Voice_factory
```

### 安装项目依赖

```powershell
python -m venv backend\.venv
backend\.venv\Scripts\python.exe -m pip install -r backend\requirements-dev.txt
npm.cmd --prefix frontend ci
```

准备音频工具：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\setup_audio_tools.ps1
```

前端依赖安装到 `frontend\node_modules`，Python 依赖安装到 `backend\.venv`。这些目录已被 Git 忽略。

### 部署本地文本分析模型

本项目的本地分析模式使用项目内的 Ollama 模型目录。执行：

```powershell
.\Start-ZwVoice.cmd setup-analyzer
```

该命令会在项目目录下准备 `local_models\ollama`，创建项目专用分析模型 `zw-voice-analyzer:4b`。安装过程中需要下载 `qwen3.5:4b`，首次操作可能耗时较长。

如果本地模型已经部署，重复执行会检查并复用现有模型。模型目录可以通过 `Start-ZwVoice.cmd status` 查看。

### 准备语音模型

启动图形启动器后，在“模型管理”中按需下载公开模型。下载目录、来源和校验信息来自：

- `config/model_catalog.json`：仓库内公开的模型目录。
- `config/model_catalog.local.json`：本机自定义镜像和额外下载项，不上传 GitHub。
- `docs/model-download-sources.md`：模型官方来源与发布边界。

模型下载器会显示来源地址，并支持已配置项目的断点续传、重试、故障转移和 SHA-256 校验。部分模型工具或轻量 TTS 资源需要按照上游项目说明手动准备；下载器显示的“手动”项目不能当成已经自动安装完成。

模型不会随 GitHub 源码仓库发布。预计占用空间取决于所选模型：GPT-SoVITS、VoxCPM2、IndexTTS2、RVC 和轻量 TTS 可以分别安装，不需要一次性全部下载。

## 4. 启动器

### 图形启动器

双击根目录的 `ZwVoiceFactoryLauncher.exe`。图形启动器提供中文界面，可用于：

- 启动、停止和查看服务状态。
- 查看模型加载进度和下载进度。
- 显示运行日志、错误信息和 API 调用阶段。
- 打开 WebUI。
- 管理模型下载源、重试和校验。

关闭图形启动器会同步关闭它托管的本地服务。

### CMD 启动器

命令行方式与图形启动器共享同一套服务所有者：

```powershell
.\Start-ZwVoice.cmd run
.\Start-ZwVoice.cmd status
.\Start-ZwVoice.cmd stop
.\Start-ZwVoice.cmd test
```

说明：

- `run`：启动 Ollama、本地 FastAPI、GPT-SoVITS、VoxCPM2、轻量 TTS、IndexTTS2 和 Vite WebUI，并打开 `http://127.0.0.1:5173/`。
- `status`：检查启动器、托管服务、端口和 WebUI 状态。
- `stop`：通过启动器关闭全部托管服务。
- `test`：以启动器托管方式运行自动化测试、前端类型检查、前端生产构建和启动器回归检查。
- `setup-analyzer`：安装或更新项目内的本地文本分析模型。

不要直接执行 Vite、FastAPI、GPT-SoVITS、VoxCPM2 或模型 Worker。绕过启动器会失去模型预加载进度、Job Object 进程回收和统一日志，容易出现“WebUI 还在但启动器不知情”的状态。

## 5. 从小说到音频

### 5.1 创建项目并导入文本

1. 打开 WebUI，在左侧“项目”区域创建项目名称。
2. 选择 `.txt` 小说文件。
3. 选择推理模板和文本分析模式。
4. 保存导入设置，确认编码、字数和章节识别结果。

项目资源会写入 `outputs\projects\` 下对应的项目目录。源码仓库不会记录这些运行资源。

### 5.2 选择推理模板

推理模板决定文本分析、切片、参考音频、导演文件和渲染的默认策略。当前文本分析有三种模式：

| 模式 | 工作方式 | 适用场景 |
| --- | --- | --- |
| 完全云端 | 文本证据直接交给已配置的云端 API 分析 | 需要更强文本理解，接受文本出站 |
| 本地初筛后云端 | 本地模型先筛角色候选和说话人线索，云端再做精析 | 长篇推荐，减少云端输入和错误候选 |
| 完全本地 | Ollama 和规则分析器在本机完成流程 | 隐私优先、无网络或成本敏感 |

混合模式不是“先把整本小说发到本地模型再发到云端”，而是尽量先在本地构造紧凑的候选证据包，再把必要片段交给云端。

### 5.3 长篇切片设置

文本达到长篇判定阈值后，小说导入页面会显示长篇设置：

- 按章节切片：适合有“第 X 章”结构的小说。
- 按字符切片：适合没有稳定章节标题的长文本，切点会尽量落在完整句边界。
- 长篇判定阈值：决定何时进入长篇处理策略。
- 每批章节：用户可以自行调整，例如 `50` 章一批。
- 每批字符：字符模式下控制单批大小。
- 后台预取：控制首个切片进入生产后，后台准备多少个后续切片。

推荐的长篇工作方式是先使用 `50` 章一批作为起点，再根据显存、云端并发和首次可播放时间调整。切片不是固定为一章一批；每批章节数由用户设置。

### 5.4 分析、审核和生成

按以下顺序处理：

1. **分析文档**：提取章节结构、角色候选、别名、证据、重要度和说话人线索。
2. **角色审核**：接受正确角色、拒绝明显误判，并可以重新生成单个角色或整体描述。
3. **标准参考**：为角色生成或上传参考文本和参考音频，试听后选择接受版本；历史版本可以翻阅、复用或删除。
4. **情绪派生**：可选择跳过，也可以生成情绪声线版本。情绪版本属于标准参考的子版本，不应替代角色身份本身。
5. **导演脚本**：为每句确定说话人、情绪、语速、停顿和表演要求；生成失败时可从失败处继续。
6. **质量渲染**：选择 GSV 版本或 IndexTTS2，逐句生成质量音频，查看真实音频波形并试听。
7. **一键质量生产**：在准备阶段直接启动连续流程。可选择是否在后台训练 RVC 稳定层。

阶段状态会写入项目缓存。再次执行分析会创建新的分析分支，不会默认覆盖旧分支；选择提取角色或生成导演文件时，可以复用先前分支中的资源。

## 6. 两条配音路线

### 质量路线

质量路线使用已审核参考音频驱动 GPT-SoVITS 或 IndexTTS2，适合重要角色和正式节目：

```text
角色审核 -> 标准参考 -> 情绪派生（可选） -> 导演脚本 -> GSV/IndexTTS2 -> 质量音频
```

质量模型在界面中按实际安装状态显示。GSV 可选版本包括 V1、V2、V2 Pro、V2 Pro Plus、V3 和 V4；不同模型的参数、显存和表现不同。

### 极速路线

极速路线使用轻量 TTS 生成基础声线，再按角色接入 RVC Identity Layer：

```text
导演脚本 -> 轻量 TTS -> 已批准的 RVC 角色身份层 -> 极速音频
```

它适合快速预览、低成本批量生成和角色数量较多的场景。未绑定 RVC 的角色会保留轻量 TTS 原声，不会因为缺少 RVC 模型而阻塞整条流程。

两条路线共享角色、导演文件、参考资产和项目缓存。用户可以先用极速路线检查文本分配，再对重点角色使用质量路线。

## 7. 流式播放与缓存

质量和极速工作台都支持逐句试听与连续流式播放：

- 单击句子右侧播放按钮：播放或生成当前句。
- 单击中间脚本句：定位右侧对应的生成记录。
- 双击中间脚本句：从该句开始建立连续播放队列。
- 当前句播放时，后台准备后续句，减少句间停顿。
- 已存在的缓存会直接复用，不必再次请求 TTS。
- 可以暂停、继续、停止或从指定句重新建立队列。

音频缓存按阶段分层：

1. Base Render：模型原始输出。
2. RVC Derivative：可选 RVC 身份层或稳定层结果。
3. Loudness Derivative：响度处理结果。

删除缓存只删除对应项目的派生文件和任务记录，不会删除小说源文件、已接受参考音频或其他项目资源。右侧结果区支持单条删除和批量删除。

## 8. RVC 使用说明

RVC 有两个用途：

- 极速路线：作为 Identity Layer，把轻量基础声线转换为角色声线。
- 质量路线：作为可选 Stability Layer，尝试减少跨句音色漂移。

在一键流程中可以打开“训练 RVC 稳定层”。打开后，参考音频生成完成时会在后台准备训练素材和训练任务；训练不会阻塞首个切片进入质量渲染。训练完成后会显示模型、进度、日志和待审核状态，用户可以试听对比后再决定是否启用。

RVC 工作台提供：

- 训练素材和参考音频检查。
- 默认训练轮次为 20 轮，可按任务调整。
- 训练进度、当前阶段和日志。
- 训练完成后的基准试听与 A/B 对比。
- 模型版本、索引文件、角色绑定和路线启用状态。

质量路线的 RVC 不应未经审核自动覆盖基础音频。若 RVC 训练或推理失败，系统保留 Base Render，并记录回退原因，继续生成和试听。

## 9. 云端 API 配置

云端分析支持多个 OpenAI-compatible API 配置。每个配置应包含：

- 名称。
- Base URL。
- API 协议（Responses 或 Chat Completions）。
- 模型名称。
- API Key。
- 优先级和启用状态。

多个配置按 P1、P2 等优先级组成故障转移队列。连接测试只发送固定的小文本探针，不会提交整本小说；真正分析时才按阶段和批次发送输入。

API Key 只保存在本机配置，不要写进 README、截图、Git 提交或 `config/model_catalog.local.json`。如果曾经把真实 Key 粘贴到公开位置，应立即撤销并重新生成。

## 10. 日志、暂停和恢复

运行日志位于：

```text
outputs\logs\
outputs\logs\rvc\
```

日志覆盖启动器、服务健康检查、模型调用、分析阶段、云端输入输出摘要和 RVC 训练。敏感信息应被脱敏，但发布日志前仍应人工检查。

长篇分析和一键生产支持：

- 暂停和继续。
- 从失败阶段继续。
- 单个切片重试。
- 跳过无法处理的切片。
- 终止当前运行。
- 删除指定阶段缓存后重新生成。

已经完成的切片、角色和参考资源会保留。不要因为某一个云端请求失败就删除整个项目目录。

## 11. 常见问题

### 启动停在启动页面

1. 执行 `.Start-ZwVoice.cmd status` 查看是启动中还是已运行。
2. 若确认需要重启，执行 `.Start-ZwVoice.cmd stop`。
3. 再执行 `.Start-ZwVoice.cmd run`。
4. 检查 `outputs\logs` 中对应时间的启动器和模型日志。

不要直接启动前端或后端，否则启动器不会拥有这些进程。

### 提示本地分析模型未安装

先关闭运行实例，再执行：

```powershell
.Start-ZwVoice.cmd stop
.Start-ZwVoice.cmd setup-analyzer
```

确认 `local_models\ollama` 位于当前项目目录，而不是 C 盘的默认 Ollama 模型目录。

### 云端分析速度慢

常见原因是输入过大、请求过碎、并发过低、结构化输出重试或 API 端点限流。建议：

- 长篇使用“本地初筛后云端”模式。
- 提高每批章节数，避免把一句话拆成一个请求。
- 将并发设置在端点允许范围内，默认并发 4，不要盲目拉满。
- 检查云端 API 的输入、输出和重试日志。
- 为失败端点配置 P2 故障转移。

### 角色数量明显过多

优先使用本地粗筛，再让云端对候选证据做精析。检查别名归并、章节权重和明显误判候选；被拒绝的候选会保留在分析审计中，但不应进入角色声线资产。

### RVC 训练失败或看不到进度

检查 RVC 工具、训练素材、显存和 `outputs\logs\rvc\`。训练任务应在 RVC 面板看到阶段和进度；质量路线即使 RVC 失败，也应保留基础质量音频并允许继续试听。

### 端口被占用

项目使用本机端口 `5173`、`8800`、`9880`、`9881`、`9882`、`9883` 和 `11435`。先执行 `status`，确认是否已有本项目实例；不要直接结束不属于本项目的进程。

## 12. 测试与诊断

完整测试必须通过启动器执行：

```powershell
.Start-ZwVoice.cmd test
```

测试链路包括：

- 启动器 Job Object 托管检查。
- 本地分析服务检查。
- 后端 pytest。
- 前端 TypeScript 类型检查。
- 前端 Vite 生产构建。
- 主题配色、界面噪声、质量渲染交互和图形启动器回归检查。

源码修改后，不要只运行 `npm run dev` 来判断完整系统是否正常；WebUI 需要由启动器与后端、模型 Worker 一起验证。

## 13. 当前实测边界

仓库中的公开截图和 README 测试记录来自以下本地文件：

- 小文本：`input\斗破苍穹测试.txt`，约 64.5 KB、33,623 字、11 章，导演文件 883 句；已验证从导入到质量试听的链路。
- 长文本：`input\斗破苍穹天蚕土豆.txt`，约 10.1 MB、5,472,278 字、1,646 章；设置为 50 章一批后生成 33 个切片，首切片先进入质量渲染，后续切片后台准备。

长文本仍可能产生过多候选角色，角色合并和误判回收还需要继续用更多体裁的黄金文本优化。音色质量、云端耗时和 RVC 效果会受模型版本、参考音频、显存、网络和输入文本影响。

## 14. 公开发布边界

以下内容不应上传到 GitHub：

- `models\`、`local_models\`、`assets\` 中的模型权重和音频资产。
- `input\` 中的小说原文。
- `outputs\` 中的项目、音频、缓存和日志。
- `.env`、API Key、个人镜像配置和本机路径。
- Python 虚拟环境、Node 依赖和 FFmpeg 可执行文件。

源码仓库只发布应用代码、配置模板、公开下载索引、文档和脱敏截图。模型许可、来源和下载说明见 `docs\model-download-sources.md`。

## 15. 相关文档

- `docs\demo\Zw_Voice_Factory_demo.mp4`：约 49 秒的脱敏流程演示视频。
- `README.md`：项目概览、能力边界、实测结果和快速入口。
- `docs\architecture.md`：核心领域模型和音频渲染数据流。
- `docs\PROJECT_RESUME_AND_INTERVIEW.md`：技术复盘、简历项目描述和面试问答。
- `docs\release-inventory.md`：发布体积审计和 GitHub 发布清单。
- `docs\model-download-sources.md`：模型来源、完整性校验和许可证边界。
